{
  "name": "ui-tests",
  "version": "1.0.0",
  "description": "ui tests",
  "scripts": {
    "test": "npx env-path -p .env.example playwright test",
    "lint": "eslint ./**/*.ts",
    "lint:fix": "eslint ./**/*.ts --fix",
    "format": "prettier ./**/*.{ts,json} --write",
    "install-drivers": "npx playwright install"
  },
  "dependencies": {
    "@playwright/test": "^1.18.0",
    "dotenv": "^12.0.3",
    "env-path": "^1.0.7"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.9.1",
    "@typescript-eslint/parser": "^5.9.1",
    "eslint": "^7.32.0",
    "eslint-plugin-playwright": "^0.7.1",
    "prettier": "^2.5.1"
  }
}
